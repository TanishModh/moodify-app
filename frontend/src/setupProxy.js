module.exports = function (app) {
  // This file is only used to prevent webpack from trying to import node modules
  // It doesn't actually set up any proxies, but its presence helps with the webpack configuration
};
